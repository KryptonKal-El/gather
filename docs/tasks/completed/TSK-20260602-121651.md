# TSK-20260602-121651: List three-dot menu — reorder, rename, and add Manage Categories

## Request
Reorder the list three-dot menu, rename "Edit" to "Edit Name, Icon & Color", add a new "Manage Categories" option, move categories out of the Edit view into its own sheet mirroring Manage Stores. Final menu order:
1. Edit Name, Icon & Color
2. Manage Stores (conditional)
3. Manage Categories (new, conditional)
4. Share Settings
5. Duplicate
6. Reset items (conditional — guest_list owner only)
7. Delete List

## Investigation Findings

### Recommended Approach
- Rename "Edit" → "Edit Name, Icon & Color" in `listOptionsMenu` in `ListDetailView.swift`
- Reorder menu items to match the spec above
- Add `@State private var showManageCategoriesSheet = false` to `ListDetailView`
- Add "Manage Categories" button in the menu, conditional on `EditListSheet.categorySupportedTypes.contains(list.type)`, presenting `CategoryEditorSheet` with `initialCategories` + `onSave` callback that calls `viewModel.updateList`
- Remove the categories `Section` from `EditListSheet.swift` and clean up its related `@State` variables

### Confidence
HIGH

### Evidence
- [CONFIRMED] `listOptionsMenu` in `ListDetailView.swift` lines 1235–1293 — current items and order (code trace)
- [CONFIRMED] `CategoryEditorSheet` in `CategoryEditorSheet.swift` is feature-complete (search, reorder, swipe-delete, Save as Default) and takes `initialCategories: [CategoryDef]` + `onSave: ([CategoryDef]) -> Void` — currently not wired to the menu (code trace)
- [CONFIRMED] Categories section lives in `EditListSheet.swift` lines 123–187 with 5 related `@State` vars (code trace)
- [CONFIRMED] `showManageStoresSheet` pattern in `ListDetailView` is the exact template to replicate (code trace)
- [CONFIRMED] `EditListSheet.categorySupportedTypes = ["grocery", "packing", "todo"]` is the correct gate for showing Manage Categories (code trace)
- [CONFIRMED] `onSave` callback needs to call `viewModel.updateList(id: list.id, ..., categories: updatedCategories)` — same path used by EditListSheet on save (code trace)

### Eliminated Approaches
- Building a new `CategoryBrowserView` — unnecessary; `CategoryEditorSheet` is already feature-complete and mirrors the Stores UX

### Scope
Medium (2 files, ~50 lines changed)

## Implementation Decisions
1. Reset items placement: keep after Duplicate, before Delete List

## Task Breakdown

### Task 1: Reorder and update the three-dot menu
Files: `ios-native/GatherLists/GatherLists/Views/Lists/ListDetailView.swift`
What I'll do:
- Rename "Edit" button label to "Edit Name, Icon & Color"
- Add `@State private var showManageCategoriesSheet = false`
- Add the `.sheet` presenter for `CategoryEditorSheet`
- Reorder menu items to: Edit Name, Icon & Color → Manage Stores → Manage Categories → Share Settings → Duplicate → Reset items → Delete List
- Wire "Manage Categories" button conditionally on `EditListSheet.categorySupportedTypes.contains(list.type)`
- Pass `initialCategories: list.categories ?? []` and `onSave` callback that calls `viewModel.updateList`

### Task 2: Remove categories section from EditListSheet
Files: `ios-native/GatherLists/GatherLists/Views/Lists/EditListSheet.swift`
What I'll do:
- Remove the categories `Section` block (lines 123–187)
- Remove the 5 related `@State` variables: `showCategoryPreview`, `categories`, `selectedCategoryForEdit`, `pendingNewCategory`, `categoryToDelete`
- Remove any `showDeleteConfirm` / `categoryToDelete` alert blocks scoped to categories
- Remove `categories` from the `viewModel.updateList` call on save (or keep passing nil/existing value)
- Clean up any now-unused imports or helper functions

## Status Timeline
- Created: 2026-06-02T12:16:51Z
- Approved: 2026-06-02T12:16:51Z
- Started: 2026-06-02T12:16:51Z
- Completed: 2026-06-02T12:45:00Z
